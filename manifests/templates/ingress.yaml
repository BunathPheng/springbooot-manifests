---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbooot                  # Ingress name
  labels:
    app: springbooot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbooot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbooot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
